/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
        API_HOST: "http://localhost:8000/api"   //API_HOST: "https://intervuer.xyz/api/",
    },
};

export default nextConfig;
